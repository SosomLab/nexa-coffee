# 2026-09-12 — 프로젝트 생성(한 세션)

## 요청(사용자)
1. 상위 폴더의 beep/clip/dir2를 참고해 크로스 플랫폼 프로젝트 1개 구성 · 무료 라이선스.
2. 트레이 아이콘: 두꺼운 원 + 안에 남은 시간 정수(시/분/초 색 구분) · 호가 잔여 비율 · 꼬리가 그라데이션으로 사라짐 · 외곽선 유지.
3. 우클릭: 무제한·12h·6h·2h·1h·30m·끄기 → 화면보호기·덮개 닫힘·최대 절전·절전 진입 방지. 첫 실행 자동 시작 옵션.
4. (추가) 타이머 종료 시 메모리 전부 회수 · nexa-shortcut처럼 실행파일·메모리 극단적 최소화 · 앱 아이콘 = 원 1/3 실선 + 남은 1/3 다른 실선 + 나머지 점 · 대기/동작 아이콘 분리 · 일/시/분 직접 설정 UI(최대한 단순).

## 판단
- **언어 = C**(DR-1). Rust는 std 바이너리 최소 수백 KB. nexa-shortcut(4.5 KB C)과 같은 원칙으로.
- **코어 libc 금지**(DR-2): Windows CRT 미링크를 지키려면 코어가 memset/sqrt/atan2를 부르면 안 된다 → 정수 atan2/sin LUT(tools 생성) · 4×4 슈퍼샘플 커버리지 · straight-alpha over 합성.
- **Linux libdbus 없이**(DR-3): SASL EXTERNAL · UNIX_FD 협상 · 마샬러/언마샬러 ~500줄. musl 정적 → libc 하나.
- **UI = 메뉴만**(DR-4): 사용자 지정 = 서브메뉴 일(0~7)·시간(0~23)·분(5단위) + "시작 — 요약". 창 코드 0.
- 24h 이상은 **일** 단위(보라) 추가 — 사용자 지정에 일이 들어와서. 값은 단위별 내림(1h59m → "1").
- 덮개 닫힘(DR-8): Windows/macOS는 관리자 정책. Linux만 logind `handle-lid-switch` block.

## 실측
| 항목 | 값 |
| --- | --- |
| Windows x64 exe(mingw · CRT 없음) | 33,280 B(아이콘 15 KB) · 임포트 kernel32/user32/shell32/gdi32 |
| Windows x86 exe | 35,840 B(libgcc 64비트 나눗셈) |
| macOS 단일 아키 / 유니버설 | 40,568 B / 119,336 B |
| macOS 상주 RSS | 21.3 MB(AppKit 하한 · 자동 시작 상태 21.5 MB) |
| test_core | 64 검사 green |
| linux-smoke(macOS brew dbus-daemon) | GetAll · Introspect · GetLayout · Get/GetGroupProperties/AboutToShow · Event(12h→툴팁 "12시간 남음" · 설정 sel=2) · 사용자 지정 1h5m · 끄기 · 종료 = green |

## 문제와 해결
- 16px 두 자리 숫자 흐림 → 글리프 칸이 1~2px 사이면 1px로 고정하고 원점을 픽셀 격자에 스냅(`icon_active.c layout_text`).
- macOS `dbus-daemon --session`은 launchd 소켓 요구 → `scripts/dbus-test.conf`(unix:tmpdir · EXTERNAL)로 띄운다. CI 컨테이너에서도 같은 설정.
- `grep -q`가 파이프를 일찍 닫아 echo SIGPIPE → `has()`로 끝까지 읽기.
- `XDG_CONFIG_HOME` 상위 폴더가 없으면 설정 저장 실패 → conf_init에서 두 단계 mkdir.
- Docker Desktop 백엔드가 이 세션에서 종료돼 컨테이너 검증은 못 했다(스크립트는 준비: `scripts/linux-docker.sh`).
- System Events로 메뉴 열기 실패(접근성 권한) — 메뉴 스크린샷은 없다. 메뉴 트리는 test_core로 검증.

## 남긴 것
- Windows·Linux 실기 미확인(TODO T-2·T-3). macOS 화면보호기 실측(T-4).

## 2차 — 커밋 · 병합 · push (사용자 요청)
- 브랜치 `feat/scaffold`(main이 unborn이라 루트 커밋부터 브랜치에서). 수직 슬라이스 9커밋: chore(repo) → feat(core) → test(core) → feat(plat/win) → feat(plat/mac) → feat(plat/linux) → build(pkg) → ci → docs.
- main으로 fast-forward 병합(공통 조상 없음 · 첫 이력) → `git push -u origin main` → 로컬 브랜치 삭제. 이력은 [BRANCHES](../BRANCHES.md).
- push 직전 재검증: `make test`(64 검사) · `make`/`make app`/`make win` · `linux-smoke.sh` 모두 green.

## 3차 — CI 첫 실행 결과
- macos ✓(12s) · windows-msvc ✓(18s · `build.bat`가 /NODEFAULTLIB로 링크 성공 — MSVC 경로 첫 확인) · linux ✗.
- linux: musl 정적 바이너리가 **실제 Linux dbus-daemon** 위에서 `linux-smoke.sh` all green(SNI·dbusmenu·Event·설정 저장·종료). 실패 원인은 스크립트 EXIT 트랩 — 이미 종료된 앱에 `kill`이 실패하고 `bash -e`가 트랩 안에서 exit 1로 만들었다(macOS sh에선 재현 안 됨). 트랩이 `$?`를 보존하도록 수정.
