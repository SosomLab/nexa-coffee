# 2026-09-13 — 메뉴 맨 위 남은 시간(1초 갱신)

## 요청(사용자)
트레이 아이콘 좌클릭 시 맨 위에 `0 day 0 hour 0 minute 0 second` 형식으로 남은 시간을 1초 단위로 갱신, 1보다 크면 복수형.

## 구현
- 코어: `CfApp.remaining_s`(플랫폼이 메뉴 생성 전 채움 · 올림) · `cf_remaining_label` · 루트 맨 위에 `CF_ID_STATUS`(비활성) + 구분선. 복수형 규칙은 문자 그대로 `v > 1` → 0은 "0 day"(사용자 확정 문구). 한국어는 단복수 없음. 무제한 = "무제한 · 절전 방지 중", 대기 = "대기 중".
- macOS: `menuWillOpen:`에서 1초 반복 타이머(common modes — 메뉴 추적 중에도 발화) → `itemWithTag:` 제목 교체 · `menuDidClose:`에서 무효화.
- Windows: `TrackPopupMenu` 전 `SetTimer(TIMER_MENU)` → `WM_TIMER`에서 `SetMenuItemInfoW` + `FindWindowW(L"#32768")` `RedrawWindow`. 열린 팝업의 재그리기는 실기에서 확인 필요(T-2).
- Linux: 셸은 메뉴 열림/닫힘을 알려주지 않으므로 `AboutToShow`/`GetLayout` 후 30초를 "열려 있음"으로 보고 `ItemsPropertiesUpdated(a(ia{sv})a(ias))`를 1초마다 보낸다(작업 중일 때만).
- 검증: test_core(+8 검사) · linux-smoke(id 14 라벨 정규식) · macOS는 `NEXA_COFFEE_SHOW=menu`(status 버튼 `performClick:`) 훅을 넣었으나 전체 화면 창이 메뉴바를 가려 캡처하지 못했다 — 사용자 화면 확인 필요.

## 2차 — 상주 메모리 최소화 · 불필요 자원 검토 (사용자 요청)
### 측정(macOS · Intel)
- RSS 21.9 MB 중 **고유 메모리(phys_footprint)는 7.4 MB** — 나머지는 dyld 공유 캐시(AppKit 등 공유 페이지, 프로세스 소유 아님). 더티의 대부분은 AppKit 초기화 malloc(TINY 2.4 · SMALL 1.8 · MEDIUM 0.7 MB).
- 실험(x86_64 · 각 2회): `malloc_zone_pressure_relief` 7.37/7.69 MB(효과 없음 · 오차) · Prohibited 정책 7.29/7.52(효과 없음 → 창 제약만 생기므로 채택 안 함) · `MallocNanoZone=0` 7.67(없음) · **`MallocSpaceEfficient=1` 5.89 MB(−1.5 MB)** · relief+SpaceEfficient 5.76.
- 적용: 바이너리가 `MallocSpaceEfficient` 없으면 세팅 후 `execv` 재실행(어느 경로로 실행해도 적용) + 번들 `LSEnvironment`(보통 재실행 없음). relief는 무해해서 시작 직후 1회.
### 불필요 자원 검토
- 링크: macOS AppKit·IOKit(+Foundation·CoreFoundation·CoreGraphics·libobjc·libSystem은 AppKit 의존) · Windows kernel32/user32/shell32/gdi32 · Linux libc(musl 정적). 뺄 것 없음.
- 미사용 함수 제거: `cf_streq`(테스트만) · `cf_unit_color`(외부 미사용 → static) · `db_w_fd`·`db_r_bool`(미사용) · `DbMsg.fds`(fd 송신 안 함).
- 정적 자원: Windows `.rsrc` 15.8 KB가 exe 37.9 KB의 40% → ICO를 **PNG 프레임**(`tools/make-ico.py` · 3.7 KB)으로. macOS icns 152 → 62 KB(1024px 프레임 제거 · 번들만). 런타임에 읽는 자원은 Windows 클래스 아이콘뿐.
- 결과: Windows x64 exe 37,888 → 26,624 B · macOS 번들 실행 footprint 5,876 KB(bare 재실행 경로 5,616 KB) · 번들 276 KB(icns 포함).
- Linux/Windows: 아이콘 버퍼를 최대치(16 KB) 대신 정확한 크기로 · 시작 직후 `malloc_trim`/`SetProcessWorkingSetSize`.

## 3차 — 커밋 · 병합 · push (사용자 요청)
- 브랜치 `feat/dialog-ui-memory`: feat(core) → feat(plat/win) → feat(plat/mac) → feat(plat/linux) → build(pkg) → docs 6커밋. main ff 병합 · push · 브랜치 삭제.
- CI 34706566010: linux ✓ 26s · macos ✓ 21s · windows-msvc ✓ 26s(MSVC도 PNG 프레임 ICO·DIALOGEX 리소스 컴파일 OK).

## 4차 — Linux 실기(GNOME 50 · ubuntu-appindicators): 트레이 아이콘 안 보임 → 등록 경쟁 수정 (사용자 QA)
### 증상
- 릴리스 빌드 실행 → 워처에 등록됨(`RegisteredStatusNotifierItems`에 있음) · SNI/dbusmenu 속성 모두 정상 응답 · 확장 ACTIVE · 로그 무오류. 그런데 패널에 아이콘이 없다(포털 스크린샷으로 확인).
### 원인(`busctl monitor`로 잡음)
- GNOME AppIndicator 확장은 `RegisterStatusNotifierItem`을 받자마자 **응답을 주기 전에** 우리 항목에 `Properties.GetAll`을 보낸다(프록시 초기화). 우리는 `register_watcher()`에서 `db_call(..., NULL, NULL)`로 등록 응답을 동기 대기하면서 그 사이에 온 `GetAll`을 버렸다 → 셸의 프록시 초기화가 끝나지 않아 항목은 "등록됨"이지만 아이콘은 생성되지 않는다.
- 스모크가 못 잡은 이유: 스모크는 워처 없이 앱에 직접 호출하므로 이 순서가 재현되지 않는다(`RequestName` 쪽은 이미 `handle`을 넘기고 있었다).
### 수정
- `register_watcher()`의 `db_call`에 `handle`을 넘겨 대기 중 도착한 요청도 처리(1줄 + 전방 선언). 재캡처: `GetAll` 응답(35 KB)이 등록 응답보다 먼저 나가고 셸이 이어서 `GetLayout`까지 가져간다.
### 실기 결과(GNOME Wayland · VMware)
- 대기 아이콘 표시 ✓ · 12시간 클릭 → 동작 아이콘(초록 링 + "12") ✓ · 메뉴 라벨 "0일 11시간 59분 59초" ✓ · logind `sleep:idle:handle-lid-switch` block ✓ · 끄기 → "대기 중" · 억제 해제 ✓ · 설정 저장 `auto=0,0,0 / custom=0,1,0` ✓.
- **관찰**: GNOME 패널 아이콘은 16px인데 우리는 22·44px 픽스맵만 보내 확장이 22→16으로 축소한다 → 두 자리 숫자가 흐릿. 16px 프레임을 픽스맵 목록에 추가하면 코어 래스터라이저의 16px 기준 선명도를 그대로 쓸 수 있다(TODO).
- 검증: test green · smoke green · Linux 51,416 B.

## 5차 — 언어 선택 메뉴(English / 한국어 / 日本語 / 中文) · i18n (사용자 요청)
### 요청
자동 시작처럼 "언어 ▸" 서브메뉴를 두고 영어·한국어·일본어·중국어를 고르게. 메모리·실행 파일 크기는 최소로.
### 구현(코어)
- `CF_LANG_EN/KO/JA/ZH` · 루트에 `CF_ID_LANG`(16, 자동 시작 바로 아래) · 자식 `CF_ID_LANG0+lang`(30~33, 라디오) · 클릭 → `CF_ACT_LANG`(저장 + 메뉴·툴팁 전부 갱신).
- **크기 최소화**: 언어별 `const char *[]` 포인터 테이블(4×40×8 B + PIE 재배치) 대신 언어당 NUL로 구분한 문자열 블록 하나(`STR_EN[]`…) → id번째까지 걸어가는 `nth()`. 포인터는 언어당 1개. 메뉴를 열 때만 문구를 만드니 O(n) 걸음은 무시할 수준. 언어 이름·설정 태그도 같은 방식(`LANG_NAME` · `LANG_TAG`, lang*3).
- 툴팁 어순 일반화: en "2 hours left" · ko "2시간 남음" · **ja "残り2時間" · zh "剩余2小时"**(ja/zh는 '남음'이 앞). 예전 en 전용 `" sec "/" min "` 테이블은 없애고 남은 시간 라벨과 같은 단위 문구(S_SEC1.. = `CF_UNIT_*` 순서)를 재사용 → en 툴팁이 "5 min left" → "5 minutes left"로 바뀜.
- 설정 `lang=ko|ja|zh|en` — **메뉴에서 고른 뒤에만** 저장(`lang_set`). 안 골랐으면 OS 로케일 자동(로케일을 바꾸면 따라간다).
- `cf_lang_of("ko_KR.UTF-8" | "ja-JP" | "zh_CN")` — 3-OS의 로케일 판별 3중 코드를 코어 하나로.
### 플랫폼
- Windows: `PRIMARYLANGID` KOREAN/JAPANESE/CHINESE → lang. `CF_ACT_LANG` = 저장 + `job_tick()`/`show_idle()`로 툴팁 갱신(메뉴는 열 때 새로 만든다). 대화상자 문구는 이미 `cf_str`로 채운다. **빌드 미확인**(이 PC에 mingw 없음 — CI에서 확인 필요).
- macOS: `preferredLanguages` → `cf_lang_of`. `CF_ACT_LANG` = 저장 + `tickNow`/`showIdle`. **빌드 미확인**(Linux PC).
- Linux: `LC_ALL/LC_MESSAGES/LANG` → `cf_lang_of`. logind 억제 사유·화면보호기 사유를 `CF_STR_WHY/KEEPING`으로 코어에서. `CF_ACT_LANG` = 저장 + `LayoutUpdated` + `NewToolTip` + `NewTitle`. "대화창 도구 없음" 알림은 Linux 전용이라 linux.c에 4개 문자열 테이블로.
### 검증
- test_core +40여 검사(서브메뉴·라디오·저장/파싱·`cf_lang_of`·ja/zh 툴팁 어순·남은 시간 라벨·**네 언어 모든 문구·라벨 비어 있지 않음 = NUL 블록 개수 검증**) green.
- linux-smoke: 언어 서브메뉴 레이아웃 → 日本語 클릭 → `lang=ja` 저장 · 종료="終了" · 맨 위 라벨 `N日 N時間 N分 N秒` · 툴팁 `残り…` → 한국어 복귀 green.
- GNOME 실기(D-Bus로 전환): 日本語/中文/English/한국어 순서로 루트 라벨·툴팁·설정이 즉시 바뀜.
- Linux 바이너리 51,416 B(변화 없음 — 문자열 +1 KB가 페이지 정렬 여유 안에 들어감).

## 6차 — 커밋 · 병합 · push · CI (사용자 요청)
- 브랜치 `feat/i18n-lang-menu`: feat(core) → feat(plat) → docs → docs(진행사항) 4커밋. main ff 병합 · push · 브랜치 삭제. push 전 게이트는 이 PC에서 가능한 것만(test · Linux 빌드 · smoke) — `make win`·`make app`은 CI로 대신.
- CI 34736693426: linux ✓ 28s · windows-msvc ✓ 23s · macos ✓ 19s. Windows/macOS의 `CF_ACT_LANG`·로케일 switch·CJK UTF-8 리터럴이 두 컴파일러에서 문제없음(실기는 T-11).
- 이 PC 메모: `gh` 없음 → GitHub REST API(`/actions/runs?branch=main`)로 확인. git 신원 없음 → `-c user.name/email`로 커밋.

## 7차 — Mac에서 pull 후 로컬 검증 · 릴리즈 빌드 실행 (사용자 요청)
- 원격 6커밋(0717da7 GNOME 등록 경쟁 수정 · i18n 4커밋 · docs) fast-forward. 로컬 변경 없음.
- 검증(Intel Mac): test_core green · `make`/`make app`(유니버설 154,216 B · 번들 276 KB · ad-hoc 서명) · `make win` x64 27,136 B / x86 29,696 B(mingw · CRT 없음 유지) · POSIX 빌드로 linux-smoke green(언어 서브메뉴 → 日本語 → 한국어 왕복 포함).
- 리뷰: 루트 메뉴 16항목에 상한 20 · 등록 대기 중 `handle` 재진입 경로(클릭 → 시작 → 비동기 ScreenSaver.Inhibit)는 세션 버스 동기 호출을 중첩하지 않음 — 문제 없음.
- 릴리즈 빌드(클린 · -Os · dead-strip · strip) 실행: pid 739 · 고유 메모리 6,004 KB · RSS 21.7 MB.
- 문서만 갱신(`docs/mac-verify` 1커밋 → main ff → push).

## 8차 — Windows 실기 첫 확인 · 상주 메모리 분석 (사용자 요청 · 사용자 QA · Windows PC)
### 환경
- Windows 11 Pro 26200 · VS 2022 Build Tools(MSVC 14.44) · make/gcc/mingw/magick 없음 → `build.bat` 경로만 가능. `gh` 있음. 사용자는 RDP 세션.
- pull: 09-13 4~7차 fast-forward(1e6fd4c → 24d4a8c). 로컬 변경 없음. CI 3-OS green(34745275126).
### 빌드 · 테스트 (MSVC 첫 로컬 실행)
- `build.bat`(vcvars64): `dist/nexa-coffee-x64.exe` **27,648 B**(CRT 미링크 · Mac mingw 27,136 B와 근접).
- `test/test_core.c` + 코어 8파일을 `cl /O1 /utf-8`로 컴파일·실행 → **all green**(i18n 네 언어 문구 검사 포함 · CJK UTF-8 리터럴·NUL 블록 MSVC OK). `test/icon_dump.c`도 MSVC로 `build/icons` PAM 45개.
- 메모: cmd 체인에서 `call build.bat`가 "not recognized" → 절대 경로로 호출해야 한다. 단일 인스턴스 뮤텍스 + exe 잠금이라 재빌드 전 실행 중 프로세스를 먼저 종료.
### 실행 · 트레이 등록
- 릴리스 exe 실행 → `HKCU\Control Panel\NotifyIconSettings`에 ExecutablePath + InitialTooltip **"Nexa Coffee — 대기 중"** 등록(한국어 UI 로케일 판별 ✓). `IsPromoted` 비어 있음 → Windows 11 기본대로 "^" 오버플로에 숨는다. 시작 직후 작업 집합 1.3 MB · 스레드 1 · 대기라 `powercfg /requests` 비어 있음(정상).
- 화면 자동 클릭(오버플로 열기)은 사용자가 거부 → 아이콘·메뉴·1초 갱신 재그리기의 시각 확인은 사용자 몫(T-2 남은 항목). 사용자가 메뉴·입력 창을 열어 본 것은 이후 모듈 목록으로 확인.
### 메모리 (사용자 QA "기능이 별게 없는데 너무 큰 메모리")
- 작업 관리자 자세히 탭 "작업 집합(메모리)" 11,452 K(공유 DLL 페이지 포함). 실측:

| 시점 | 작업 집합 | 비고 |
| --- | --- | --- |
| 시작 중 최고치 | 10.3 MB | shell32 → windows.storage.dll(8.9 MB) · combase 초기화 = `Shell_NotifyIcon` 비용 |
| 시작 직후(반납 후) | 1.3 MB | 개인 작업 집합 476 K · 커밋 1.7 MB · 모듈 27 |
| 메뉴·입력 창 사용 후 | 11.5 MB 최고치 | **반납 지점이 없어 그대로 유지** → 사용자가 본 값. 모듈 37(textinputframework·CoreUIComponents·CoreMessaging·TextShaping·wintypes·OLEAUT32·dwmapi = edit 컨트롤의 TSF 스택, 내려가지 않음) |

- PNG 프레임 ICO(663b14a)의 부작용: 시작 시 `LoadIconW(IDI_APP)`가 **WindowsCodecs.dll(2.2 MB) + IconCodecService.dll**을 올린다. 8206e27의 BMP ICO로 실험 빌드(38,912 B) → 두 DLL 미로드(모듈 26) · 최고치 10,168 K로 총량 차이는 작음(mscms·icm32는 gdi32 쪽이라 남음). 디스크 11 KB ↔ 런타임 DLL 2개의 맞교환.
- 조치안 → **T-13**: ① 메뉴 닫힘·입력 창·About 뒤 `SetProcessWorkingSetSize(-1,-1)`(DR-7 확장 · 작업 관리자 값이 항상 1~2 MB) ② 클래스 아이콘을 ICO 대신 `cf_icon_idle`로 생성해 런타임 ICO 디코드 제거(PNG ICO는 탐색기 표시용으로만) ③ shell32 체인·TSF 스택은 불가피(공유 페이지라 ①로 작업 집합에서는 빠짐).
- 비교: 같은 화면의 nexa-beep 8 MB · nexa-clip 21 MB. coffee는 반납 직후 1.3 MB.
### 마무리
- 사용자 보고: 3-OS 빌드·테스트 완료(Mac 7차 + 이 PC Windows). 문서만 갱신 → `docs/win-verify` 1커밋 → main ff → push.

## 9차 — Mac: pull(8차) · 릴리즈 빌드 재시작 · 실행 용량/메모리 최소화 점검 (사용자 요청)
### 실행 파일
- 유니버설 154,216 B = x86_64 슬라이스 55,200 + arm64 88,680(코드 __TEXT 30.4 KB vs 33.7 KB로 비슷하나 arm64는 세그먼트 정렬이 16 KB라 커진다 · 정렬은 줄일 수 없음). 번들 276 KB(icns 64 KB). 더 뺄 것 없음.
### 메모리(Intel · footprint)
| 상태 | 고유 메모리 | 비고 |
| --- | --- | --- |
| 시작 직후 대기 | **6.1 MB** | MALLOC 3.5 MB · __DATA_DIRTY 0.6 MB(공유 캐시) · IOAccelerator/CA 0.2 MB |
| 메뉴 열기 → 닫기 | 8.7 → 8.3 MB | +2.2 MB 잔류 |
| About 열기 → 닫기 | 9.0 → 9.0 MB | +2.9 MB 잔류 |
| 입력 창 열기 → 닫기 | **11.3 → 11.3 MB** | +5.2 MB 잔류(NSTextField = 텍스트 시스템·글꼴 캐시) |
- Windows 8차 QA와 **같은 구조의 문제**: UI를 한 번 쓰면 AppKit 내부 캐시가 남아 대기 메모리가 2배가 된다.
- 실험: 창·메뉴 닫힌 뒤 `malloc_zone_pressure_relief` → 입력 창 10.2 MB(−1 MB) · About/메뉴 변화 없음. **효과 미미 → 미채택.** free-list가 아니라 프레임워크가 쥔 객체·캐시라 프로세스 안에서 돌려줄 수 없다.
### 결론 · 제안(T-14)
- **창(입력 창·About)을 자식 프로세스에서 띄운다**: 자기 자신을 `--dialog custom|auto` / `--about`로 실행 → 결과를 stdout 파이프로 받고 자식은 종료 → 텍스트 시스템·글꼴 캐시가 프로세스와 함께 사라져 부모는 6 MB 유지. Windows도 동일(`CreateProcess` 자기 자신 · TSF 스택 제거 = T-13의 근본 해법) · Linux는 이미 zenity 자식 프로세스라 **3-OS 같은 구조**가 된다.
- 메뉴(+2.2 MB)는 프로세스 안에서 추적해야 하므로 남는다 — 허용(대기 8 MB대).
- Windows T-13 ②(클래스 아이콘을 `cf_icon_idle`로 생성해 WindowsCodecs 로드 제거)도 같이.

## 10차 — T-14 구현: 창을 자식 프로세스로 (사용자 요청 "구현해줘")
- 코어: `cf_parse_dhm`(공백·'|'·소수부 허용 · 3-OS 공용) + 테스트 5건.
- macOS: `main()`이 `--dialog custom|auto d h m lang` / `--about lang`이면 `child_main` → `UiChild`(기존 NSPanel/About 코드를 옮김) → stdout "d h m" · 종료 코드 0/1. 부모는 `NSTask` + `NSPipe` · terminationHandler에서 메인 큐로 `childDone`. `Coffee`에서 창 코드·NSWindowDelegate 제거.
- Windows: `start()` 첫 줄에서 `child_main(GetCommandLineW())`(뮤텍스 전) · 자식은 `DialogBoxParamW(NULL 부모)` 또는 `MessageBoxW` 후 `ExitProcess`. 부모 `spawn_ui`: `CreateProcessW` + 상속 파이프 · `RegisterWaitForSingleObject` → `WM_CHILD_DONE` → `ReadFile`·`GetExitCodeProcess` → `cf_parse_dhm`. T-13 ① 메뉴 닫힘·자식 종료 뒤 `SetProcessWorkingSetSize(-1,-1)` · ② `LoadIconW` 제거(창 아이콘은 `cf_icon_idle` 16px).
- Linux: `dialog_finish`가 `cf_parse_dhm` 사용(중복 파서 제거).
- 실측(macOS · 자동화는 키 입력 없이 자식 kill로 취소 경로만): 입력 창 열림 — 부모 **6,008 KB** · 자식 12,288 KB → 자식 종료 후 부모 5,884 KB. About — 부모 5,876 → 5,716 KB. 이전(in-process)엔 11.3 MB 잔류. **시작 버튼 경로(파이프 → 작업 시작)는 사용자가 클릭해 확인 필요**(파서는 단위 테스트).
- 크기: Windows x64 exe 27,136 → 29,184 B(자식 프로세스 배관 +2 KB) · x86 32,256 B · 스모크 green.

## 11차 — v0.1.0 릴리스 · brew/winget/choco 배포 파이프라인 (사용자 요청)
- 이식(nexa-clip): `packaging/render-manifests.sh`(자산 sha256 자동 치환 · 남은 자리표시자 있으면 실패) · `homebrew.yml`(TAP_TOKEN으로 kiros33/homebrew-tap에 Casks/nexa-coffee.rb + Formula/nexa-coffee-portable.rb 커밋) · `publish-windows-packages.yml`(변수+시크릿 이중 게이트 · winget 열린 PR/choco 직전 버전 미공개면 대기 · `choco pack/push` · `winget validate` + `wingetcreate submit`). `release.yml`이 publish 뒤 둘을 호출.
- 산출물 규약 정리: Windows zip 루트 = exe + LICENSE(`zip -j`) · Linux tar 최상위 = `nexa-coffee-<v>-linux-x64/` · mac zip = `Nexa Coffee.app` + LICENSE.
- 채널 정의: winget `SosomLab.NexaCoffee`(zip/portable · x64+x86 · alias nexa-coffee) · choco `nexa-coffee`(Install-ChocolateyZipPackage · `.gui` shim · beforemodify로 프로세스 종료) · brew Cask(유니버설 zip · postflight quarantine 제거 · caveats) + Linux Formula(정적 tar.gz).
- 저장소: 시크릿 3종은 09-12에 이미 등록돼 있었다 · 변수 `WINGET_PUBLISH=true`·`CHOCO_PUSH=true` 설정.
- 로컬 검증: 로컬 빌드로 릴리스와 같은 이름의 자산 4개를 만들어 render → ruby -c(cask·formula) · YAML 파싱 · zip 내용(루트 exe+LICENSE) 확인.
