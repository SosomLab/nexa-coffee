# 셸 스크립트는 어느 OS에서 체크아웃해도 LF(Windows autocrlf가 CRLF로 바꾸면 bash가 죽는다 — nexa-clip 선례)
*.sh text eol=lf
Makefile text eol=lf
# Windows 배치는 CRLF
*.bat text eol=crlf
# 바이너리
*.png binary
*.ico binary
*.icns binary
